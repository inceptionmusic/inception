import { Stack, Typography } from '@mui/material'

export default function Dashboard() {
  return (
    <Stack
      justifyContent='center'
      alignItems='center'
      maxWidth={1200}
      height={1}
    >
      <Typography variant='h1'>Welcome to the Inception dashboard</Typography>
    </Stack>
  )
}
